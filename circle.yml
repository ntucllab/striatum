machine:
	timezone:
		Asia/Taipei

	environment:
		CIRCLE_ENV: test

	post:
		- pyenv global 2.6.7 2.7.11 3.4.2 3.5.2

dependencies:
	pre:
		- pip install -U pip wheel && pip install -U Cython && pip install -Ur requirements.txt

test:
	override:
		- python setup.py nosetests
